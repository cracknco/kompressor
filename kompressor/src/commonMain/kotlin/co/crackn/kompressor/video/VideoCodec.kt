package co.crackn.kompressor.video

/** Supported video codecs for compression. */
enum class VideoCodec {
    /** H.264 / AVC — widely supported hardware-accelerated codec on both Android and iOS. */
    H264,
}
