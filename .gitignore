*.iml
.gradle
.idea
.DS_Store
build
captures
.externalNativeBuild
.cxx
local.properties
xcuserdata
.kotlin
#So we don't accidentally commit our private keys
*.gpg
# Firebase / GCP service account JSON — should only ever live in a GitHub secret,
# never on disk inside the repo.
*firebase-adminsdk*.json
*-service-account*.json

# XcodeGen-generated artifacts — regenerated from project.yml in CI.
iosDeviceSmokeTests/*.xcodeproj
iosDeviceSmokeTests/SmokeTests/

# Local planning + agent worktree state — not part of the repo contract.
.claude/
task_plan.md
findings.md
progress.md

